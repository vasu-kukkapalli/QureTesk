import { rdxFetch } from "../config/axiosConfig";

export const USER_LOGIN = 'USER_LOGIN'
export const USER_SIGNUP = 'USER_SIGNUP'

export const putSignupDetails = data => async dispatch => {
    // const response = await rdxFetch(SIGNUP_DETAILS, {
    //     url: `/users/auth/signup`,
    //     postData: data
    // });
    // dispatch(response);
};

export const putSigninDetails = data => async dispatch => {
    // const response = await rdxFetch(SIGNIN_DETAILS, {
    //     url: `/users/auth/signin`,
    //     postData: data
    // });
    // dispatch(response);
};